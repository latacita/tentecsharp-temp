using System;
using System.Collections.Generic;
// This is the content of the class named "Gateway", which is 
// in the package: "MyHome", from the "My_Data" model. 
namespace My_Data{
	public class Gateway{ 
		// Constructor and methods from the from the current class		
		private  virtual void switchDown ( ) {}
		protected  virtual void checkTime ( ) {}
		private void Gateway ( ) {}
		private  virtual void heaterAdjustTemperature ( Int id, double temp ) {}
		private  virtual void openWindow ( Int id, Int degree ) {}
		private  virtual void checkAll ( ) {}
		private  virtual void adjustTemperature ( Int id_heater, double temperature ) {}
		protected  virtual void findEmptyTime ( ) {}
		private  virtual void initializeAll ( ) {}
	
	 
	}
} 
  